# Copy to secrets.env and fill in. Do NOT commit secrets.env.

# OpenClash dashboard password (Luci / :9090 UI)
OPENCLASH_DASHBOARD_PASS=CHANGE_ME

# Airport Clash Meta subscription URL (flag=clash)
SUB_URL=
SUB_NAME=ssLinks
