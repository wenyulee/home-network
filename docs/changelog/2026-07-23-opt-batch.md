# 2026-07-23 — 优化批次（Firstrade 分流 / 去重 / DNS / 订阅入口）

## 已做

1. **Firstrade 精细分流**：`api3x` / `streamingx` / `invest` → `手动选择`；其余 `firstrade.com|net` → DIRECT。
3. **B AppleMedia 去重**：overwrite 不再注入 AppleMedia/Firstrade（只留 Gmail）；规则只在 `custom_rules.list`。
4. **订阅入口统一清洗**：`task.user` 中 104 与 204 均指向 `update_sub_clean.sh`。
5. **hosts / dnsmasq IP 校准**（DoH 2026-07-23）：`www` → `76.76.21.93`；CF 主机仍用现答中仍有效的钉选。
6. **dnsmasq 抗污染**：`filter-AAAA` + firstrade 钉选；对 google/github 等加了 `server=/…/1.1.1.1`。注意：本 ISP 对部分域名连查 1.1.1.1 仍可能被污染，LAN 主路径仍依赖 Clash Fake-IP/DoH。
7. **ShellCrash 升级**：本地 1.9.4release，远端 1.9.5**beta1** — **不升**。
8. **post_sub_clean auth**：从 `ShellCrash.cfg` 读取。

**B 坑**：overwrite 的 `reject!` 若匹配 `AppleMedia`/`firstrade`，会把 `custom_rules` 注入的规则一并删掉；现只清理 Gmail 相关。

## 关于「自动选择测速」（未改，说明）

不是「多塞几个 URL 轮询」。现在整组 `url-test` 只测 `cp.cloudflare.com/generate_204`，选延迟最低节点。  
若要优化流媒体/券商，应**另建**策略组（例如「香港测速」），用更贴近业务的 URL 或只放港节点，再让 AppleMedia/Firstrade 指向该组——与全局「自动选择」分开。需要时再做。
