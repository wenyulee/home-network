# 2026-07-22〜23 复盘摘要

完整可视化：[home-network-retro canvas](../../../.cursor/projects/Users-wenyulee-Projects-home/canvases/home-network-retro-2026-07-23.canvas.tsx)（Cursor 侧栏打开）。

## 五类问题 → Solution

| ID | 问题 | Solution |
|----|------|----------|
| P1 | 订阅清洗误删策略组 → 04:00 回滚 | 只删假节点 `name:`；sed 清组内引用；task 204 |
| P2 | Firstrade/api3x CloudFront HKG 直连差 | DNS 钉选 + `filter-AAAA`；域名 → `手动选择` |
| P3 | Apple TV 卡 vs 整组 Apple | 仅 `AppleMedia → 手动选择` |
| P4 | AAAA Happy Eyeballs | A `filter-AAAA`；B 关 LAN/WAN IPv6 |
| P5 | `user.yaml` 顶层 `ipv6` 重复键 | 只设 `dns.ipv6`；勿顶层 `ipv6: false` |

## 稳态

- 自动选择：`url-test` / CF 204 / 300s / tolerance 20 / lazy  
- 真节点 ≈ 91（dashboard 多出的多为 Expire/Traffic/Sync 信息行）
